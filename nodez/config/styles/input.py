from toga.style.pack import Pack
from toga.colors import BLUE, BLACK, WHITE


class InputStyle():
    
    rpcuser_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    rpcpassword_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    rpcport_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    rpcbind_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    rpcclienttimeout_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    rpcallowip_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    rpcconnect_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    proxy_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    bind_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    whitebind_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    maxconnections_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    addnode_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    connect_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    paytxfee_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    genproclimit_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    equihashsolver_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    keypool_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )

    exportdir_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )