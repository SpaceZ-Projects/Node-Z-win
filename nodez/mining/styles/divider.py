from toga.style.pack import Pack
from toga.constants import CENTER

class DividerStyle():
    
    start_divider_top = Pack(
        padding_bottom = 10
    )
    
    download_divider_top = Pack(
        padding_bottom = 5
    )
    
    wizard_divider = Pack(
        width = 550
    )
    
    wizard_divider_center = Pack(
        padding_top = 3
    )
    
    local_divider = Pack(
        height = 40,
        padding_top = 28,
        padding_left = 5
    )

    rpc_divider = Pack(
        padding_top = 10
    )

    divider_params = Pack(
        height = 255,
        padding_top = 4,
        padding_left = 10
    )