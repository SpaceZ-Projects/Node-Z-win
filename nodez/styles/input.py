from toga.style.pack import Pack
from toga.colors import BLUE, BLACK, WHITE


class InputStyle():
    
    connect_rpcuser_input = Pack(
        font_family = "monospace",
        color = BLUE
    )
    
    connect_rpcpassword_input = Pack(
        color = BLUE
    )
    
    connect_rpchost_input = Pack(
        font_family = "monospace",
        color = BLUE
    )
    
    connect_rpcport_input = Pack(
        padding_bottom = 5,
        font_family = "monospace",
        color = BLUE
    )
    
    config_rpcuser_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_rpcpassword_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_rpcport_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_rpcbind_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_rpcclienttimeout_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_rpcallowip_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_rpcconnect_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_proxy_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_bind_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_whitebind_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_maxconnections_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_addnode_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )
    
    config_connect_input = Pack(
        padding_top = 2,
        color = WHITE,
        background_color = BLACK
    )